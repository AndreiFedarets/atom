<UserControl x:Class="Atom.Client.Win.Views.DocumentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             HorizontalAlignment="Stretch" VerticalAlignment="Stretch" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" Background="White">
    <Grid>
        <TabControl ItemsSource="{Binding Items}" SelectedItem="{Binding ActiveItem}">
            
        </TabControl>
    </Grid>
</UserControl>
